\chapter{Requirements}

The project's primary objective is to develop multiple system archietctures to enable live software updates in a running control real-time system. 

Framework for developers to run software components which can be dynamically updated

Example application that uses one differential equation and requires its state to be maintained with a plant for the example application to test it's functionality

Testing harness that can measure th


\textbf{High-Level}
During operation

The software infrastucture must 
    provide the functionality to run software components with a cyclic schedule
        allow users to create/start/stop/remove software components during operation
        allow users to change the configured cyclic schedule during operation

    provide the functionality to allow components to comunicate using messages passing
        allow users to configure communications between components using routes and channels
        allow users to add/remove routes between software components during operation

\textbf{Low-Level}  
    create
        - loads the software component binary
        - starts the software component 

    start
        - marks the software component as ready to run

    stop
        - marks the software components as not ready to run

    stop
        - 

The example application must 
    maintained

The plant must
    model a mass falling due to gravity

    
