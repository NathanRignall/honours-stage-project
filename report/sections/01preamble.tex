\RequirePackage{amsmath}
\RequirePackage{amssymb}
\RequirePackage{xspace}

% tikz drawing
\usepackage{tikz}
\usetikzlibrary{automata, positioning, arrows, shadows, fit, calc, matrix}
\tikzset{block/.style={draw, thick, text width=3cm, minimum height=1.5cm, align=center}}  

% tikz uml thread diagrams
\usepackage{pgf-umlsd}

% tikz uml diagrams
\usepackage{pgf-umlcd}
\renewcommand{\umltextcolor}{black}
\renewcommand{\umldrawcolor}{black}
\renewcommand{\umlfillcolor}{white}

% tikz schedule diagram
\usepackage{tikz-timing}[2014/10/29]
\usepackage{xparse}
\usetikztiminglibrary[rising arrows]{clockarrows}

% special command for shifted
\newcommand{\newthreadShift}[4][gray!30]{
  \newinst[#4]{#2}{#3}
  \stepcounter{threadnum}
  \node[below of=inst\theinstnum,node distance=0.8cm] (thread\thethreadnum) {};
  \tikzstyle{threadcolor\thethreadnum}=[fill=#1]
  \tikzstyle{instcolor#2}=[fill=#1]
}

% timing diagrams
\usetikzlibrary{decorations.pathreplacing}

% command to draw bus
\NewDocumentCommand{\busref}{som}{\texttt{%
#3%
\IfValueTF{#2}{[#2]}{}%
\IfBooleanTF{#1}{\#}{}%
}}

% lists
\usepackage{enumitem} %% for effortless customization of enumeration
\usepackage{hyperref} %% Just to provide clickable links
\usepackage{cleveref} %% for clever referencing
\usepackage{mdwlist}
\newcommand{\randomtext}{Hello, here is some text without a meaning. Hello, here is some text without a meaning.}
  \newcommand{\shortrandomtext}{Hello, here is some text.}

%  code previews
\usepackage{listings}

% tikz trees
\usetikzlibrary{trees}

\usepackage{subcaption}
\usepackage{booktabs,amssymb,makecell,rotating}

\renewcommand*\theadfont{\bfseries}
\settowidth\rotheadsize{\theadfont Infrastructure}
\renewcommand\theadgape{}
\renewcommand\theadalign{lc}
\renewcommand\rotheadgape{}

\usepackage{atbegshi,picture}
\immediate\write18{git rev-parse --short HEAD > githash.aux }
\AtBeginShipout{\AtBeginShipoutUpperLeft{%
  \put(\dimexpr\paperwidth-1cm\relax,-1.5cm){\makebox[0pt][r]{\framebox{DRAFT \input{githash.aux}}}}%
}}